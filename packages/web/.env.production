# Ganti URL ini dengan alamat asli Worker Cloudflare Anda!
# (Bisa dilihat di dashboard Cloudflare Workers)
VITE_API_URL=

# Canonical public URL of the deployed site (no trailing slash).
# Set the real value in Cloudflare Pages → Settings → Environment variables.
VITE_PUBLIC_URL=
